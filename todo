Gen ssh-keys
checkout startupscript and it's commands 
autocomfirm everything (yay, pacman and nvchad)
maybe more configs
Why it sometimes does not shutdown properly
Add to bar if caps lock is pressed
Add autorandr
